services:
  - type: web
    name: marclite-web
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt && pip install -e engine
    startCommand: uvicorn web.app:app --host 0.0.0.0 --port 10000
